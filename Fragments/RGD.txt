lp = game.Players.LocalPlayer
local TweenService = game:GetService("TweenService")

Data = {
    ScriptName = "SimpleTab Example", -- The name of your script!
    
    Color = Color3.fromRGB(255,255,255),
    DarkC = Color3.fromRGB(255 /2.5,255 /2.5,255 /2.5),
    BgColor = Color3.fromRGB(10,10,10),
    Font = Enum.Font.Arcade,
    TextColor = Color3.fromRGB(255,255,255),
    
    OnoffButton = true, --Adds a button/keybind that reenables the gui
    OnoffKeybind = "M",
    IsZ = true
}

local SimpleTab = loadstring(game:HttpGet('https://raw.githubusercontent.com/Zeuxtronic/Zeouron/main/SimpleTab%20Raw.lua'))()

function fly(speed)
	 local controlModule = require(game.Players.LocalPlayer.PlayerScripts:WaitForChild('PlayerModule'):WaitForChild("ControlModule"))
	 local bv = Instance.new("BodyVelocity")
	 bv.Name = "VelocityHandler"
	 bv.Parent = game.Players.LocalPlayer.Character.HumanoidRootPart
	 bv.MaxForce = Vector3.new(0,0,0)
	 bv.Velocity = Vector3.new(0,0,0)
	 
	 local bg = Instance.new("BodyGyro")
	 bg.Name = "GyroHandler"
	 bg.Parent = game.Players.LocalPlayer.Character.HumanoidRootPart
	 bg.MaxTorque = Vector3.new(9e9,9e9,9e9)
	 bg.P = 1000
	 bg.D = 50
	 
	 Signal1 = game.Players.LocalPlayer.CharacterAdded:Connect(function(NewChar)
	 local bv = Instance.new("BodyVelocity")
	 bv.Name = "VelocityHandler"
	 bv.Parent = NewChar:WaitForChild("Humanoid").RootPart
	 bv.MaxForce = Vector3.new(0,0,0)
	 bv.Velocity = Vector3.new(0,0,0)
	 
	 local bg = Instance.new("BodyGyro")
	 bg.Name = "GyroHandler"
	 bg.Parent = NewChar.HumanoidRootPart
	 bg.MaxTorque = Vector3.new(9e9,9e9,9e9)
	 bg.P = 1000
	 bg.D = 50
	 end)
	 
	 local camera = game.Workspace.CurrentCamera
	 
	 Signal2 = game:GetService"RunService".RenderStepped:Connect(function()
	 if game.Players.LocalPlayer.Character and game.Players.LocalPlayer.Character:FindFirstChildOfClass("Humanoid") and game.Players.LocalPlayer.Character.Humanoid.RootPart and game.Players.LocalPlayer.Character.HumanoidRootPart:FindFirstChild("VelocityHandler") and game.Players.LocalPlayer.Character.HumanoidRootPart:FindFirstChild("GyroHandler") then
	 
	 game.Players.LocalPlayer.Character.HumanoidRootPart.VelocityHandler.MaxForce = Vector3.new(9e9,9e9,9e9)
	 game.Players.LocalPlayer.Character.HumanoidRootPart.GyroHandler.MaxTorque = Vector3.new(9e9,9e9,9e9)
	 game.Players.LocalPlayer.Character.Humanoid.PlatformStand = true
	 
	 game.Players.LocalPlayer.Character.HumanoidRootPart.GyroHandler.CFrame = camera.CoordinateFrame
	 local direction = controlModule:GetMoveVector()
	 game.Players.LocalPlayer.Character.HumanoidRootPart.VelocityHandler.Velocity = Vector3.new()
	 if direction.X > 0 then
	 game.Players.LocalPlayer.Character.HumanoidRootPart.VelocityHandler.Velocity = game.Players.LocalPlayer.Character.HumanoidRootPart.VelocityHandler.Velocity + camera.CFrame.RightVector*(direction.X*speed)
	 end
	 if direction.X < 0 then
	 game.Players.LocalPlayer.Character.HumanoidRootPart.VelocityHandler.Velocity = game.Players.LocalPlayer.Character.HumanoidRootPart.VelocityHandler.Velocity + camera.CFrame.RightVector*(direction.X*speed)
	 end
	 if direction.Z > 0 then
	 game.Players.LocalPlayer.Character.HumanoidRootPart.VelocityHandler.Velocity = game.Players.LocalPlayer.Character.HumanoidRootPart.VelocityHandler.Velocity - camera.CFrame.LookVector*(direction.Z*speed)
	 end
	 if direction.Z < 0 then
	 game.Players.LocalPlayer.Character.HumanoidRootPart.VelocityHandler.Velocity = game.Players.LocalPlayer.Character.HumanoidRootPart.VelocityHandler.Velocity - camera.CFrame.LookVector*(direction.Z*speed)
	 end
	 end
	 end)
 end
 
 function unfly()
	 game.Players.LocalPlayer.Character.HumanoidRootPart.VelocityHandler:Destroy()
	 game.Players.LocalPlayer.Character.HumanoidRootPart.GyroHandler:Destroy()
	 game.Players.LocalPlayer.Character.Humanoid.PlatformStand = false
	 Signal1:Disconnect()
	 Signal2:Disconnect()
 end

local antivoid = Instance.new("Part", game.Workspace)
antivoid.Transparency = 1
antivoid.Size = Vector3.new(2048, 0.1, 2048)
antivoid.Position = Vector3.new(0, -2, 0)
antivoid.Anchored = true
antivoid.CanCollide = false

LocalPlayer = SimpleTab.NewTab("LocalPlayer")
Droid = SimpleTab.NewTab("Droid")
Room = SimpleTab.NewTab("Room")
World = SimpleTab.NewTab("World")
Render = SimpleTab.NewTab("Render")

Render.NewSwitch("Esp Enemies",function(bool)
    EspEnemies = bool
end)

Render.NewSwitch("Esp Allies",function(bool)
    EspAllies = bool
end)

Render.NewSwitch("Esp Players",function(bool)
    EspPlayers = bool
end)

LocalPlayer.NewSlider("WalkSpeed", {1,100}, lp.character.Humanoid.WalkSpeed,function(number)
    lp.character.Humanoid.WalkSpeed = number
end)

LocalPlayer.NewSlider("JumpPower", {25,150}, lp.character.Humanoid.JumpPower,function(number)
    lp.character.Humanoid.JumpPower = number
end)

flyspeed = 40
LocalPlayer.NewSlider("Fly Speed", {1,100}, 40,function(number)
    flyspeed = number
end)

LocalPlayer.NewSwitch("Fly",function(bool)
    if bool then
        fly(flyspeed)
    else
    	unfly()
    end
end)

LocalPlayer.NewSwitch("Autoclicker",function(bool)
    autoclick = bool
    while wait() and autoclick do
        if lp.character:FindFirstChildWhichIsA("Tool") then
            local CurrentTool = lp.character:FindFirstChildWhichIsA("Tool")
            if CurrentTool:FindFirstChild("SwordScript") then
                CurrentTool:Activate()
            end
        end
    end
end)

LocalPlayer.NewSwitch("AutoEquipBest",function(bool)
    AEB = bool
    while wait(0.05) and AEB do
        if lp.character:FindFirstChildWhichIsA("Tool") then
            print("found")
            if lp.character:FindFirstChildWhichIsA("Tool"):FindFirstChild("SwordScript") then
                print("new")
                local CurrentTool = lp.character:FindFirstChildWhichIsA("Tool")
                local DamageCombined = CurrentTool.DamageValues.Lunge.Value + CurrentTool.DamageValues.Slash.Value
                local Highest = 0
                local HighestTool = nil
                for i,v in pairs(lp.Backpack:GetChildren()) do
                    if v:FindFirstChild("SwordScript") then
                        local LocalDamageCombined = v.DamageValues.Lunge.Value + v.DamageValues.Slash.Value
                        if LocalDamageCombined > Highest then
                            Highest = LocalDamageCombined
                            HighestTool = v
                        end
                    end
                end
            	if Highest > DamageCombined and HighestTool.Name ~= CurrentTool.Name then
                 	print("change")
            		lp.character.Humanoid:EquipTool(HighestTool)
             	end
            end
        end
    end
end)

LocalPlayer.NewSwitch("AutoHeal",function(bool)
    autoh = bool
    local healitems = {
        {"Bandages", 20},
        {"Medkit", 50},
        {"Cherry Pie", 25},
        {"Apple Pie", 25},
        {"Potion of Sustaining", 25},
        {"Potion of Regeneration", 50},
        {"Potion of Healing", 50},
        {"Potion of Chance", 25},
        {"Potion of Sustaining", 25},
        {"Fervor Berries", 10},
        {"Waffles", 25},
        {"Space Sandwich", 50},
        {"Pink Dount", 25}
    }
    while wait() and autoh and lp.character.Health ~= lp.character.Humanoid.MaxHealth do
        for i,v in pairs(healitems) do
            if lp.Backpack:FindFirstChild(v[1]) and lp.character.Humanoid.Health ~= lp.character.Humanoid.MaxHealth then
        		if lp.character.Humanoid.Health < lp.character.Humanoid.MaxHealth-v[2] or lp.character.Humanoid.Health < 25 then
              		local oldtool = lp.character:FindFirstChildWhichIsA("Tool")
              		lp.character.Humanoid:EquipTool(lp.Backpack:FindFirstChild(v[1]))
                	lp.character:WaitForChild(v[1]):Activate()
                 	lp.character.Humanoid:EquipTool(oldtool)
              	end
            end
        end
    end
end)

LocalPlayer.NewSwitch("Enable PotionUI",function(bool)
    if bool then
        if not lp.PlayerGui:FindFirstChild("PotionCraftingUI") then
        	local ui = game.ReplicatedStorage.SafeUI.PotionCraftingUI:Clone()
         	ui.Parent = lp.PlayerGui
          	ui.Enabled = true
        end
    else
    	 if lp.PlayerGui:FindFirstChild("PotionCraftingUI") then
         	lp.PlayerGui:FindFirstChild("PotionCraftingUI"):Destroy()
         end
    end
end)

LocalPlayer.NewButton("SwordReach",function()
    if lp.character:FindFirstChildWhichIsA("Tool") then
        lp.character:FindFirstChildWhichIsA("Tool").Handle.Size = Vector3.new(lp.character:FindFirstChildWhichIsA("Tool").Handle.Size.x, lp.character:FindFirstChildWhichIsA("Tool").Handle.Size.y, 25)
    end
end)

Droid.NewButton("Kill droids",function()
    game.Workspace.Gravity = 0
	local currentroom = game.Workspace:FindFirstChild("Room")
	for i,v in pairs(currentroom.Enemies:GetChildren()) do
     	if v:FindFirstChildWhichIsA("Humanoid") and v.Name ~= "Artic Droid" and v.Name ~= "Burner Droid" and v.Name ~= "Circuit Breaker" then
     	if djm then
      		v:BreakJoints()
     	else
      		v.Enemy.Health = 0
        end
     	lp.character.HumanoidRootPart.CFrame = CFrame.new(v.HumanoidRootPart.Position.x, v.HumanoidRootPart.Position.y +10, v.HumanoidRootPart.Position.z)
      	wait(0.5)
       	end
	end
	game.Workspace.Gravity = 193
end, "Kills all droids in the (current) room")

Droid.NewSwitch("Auto Kill all",function(bool)
    aka = bool
    if bool then
        while wait() and aka do
			local currentroom = game.Workspace:FindFirstChild("Room")
   			if currentroom:FindFirstChild("Enemies") then
			for i,v in pairs(currentroom.Enemies:GetChildren()) do
       			if v:FindFirstChildWhichIsA("Humanoid") and v.Name ~= "Artic Droid" and v.Name ~= "Burner Droid" and v.Name ~= "Circuit Breaker" then
      				v.Enemy.Health = 0
       			end
    		end
  			end
  		end
	end
end,"Kills all droids automatically")

Droid.NewSwitch("Auto Kill Shopkeepers",function(bool)
    aks = bool
    if bool then
        while wait(3) and aks do
			local currentroom = game.Workspace:FindFirstChild("Room")
   			if currentroom:FindFirstChild("ShopEvent") then
				for i,v in pairs(game.Workspace.PassiveDroids:GetChildren()) do
        			if v:WaitForChild("Tags"):FindFirstChild("Shopkeeper") then
               			v:WaitForChild("Enemy").Health = 0
               		end	
        		end
  			end
  		end
	end
end,"Kills all droids automatically")

Droid.NewSwitch("Freeze droids",function(bool)
    fd = bool
    if bool then
        while wait() and fd do
			local currentroom = game.Workspace:FindFirstChild("Room")
			for i,v in pairs(currentroom:WaitForChild("Enemies"):GetChildren()) do
       			if v:FindFirstChildWhichIsA("Humanoid") then
    				v.Enemy.WalkSpeed = 0
       			end
    		end
  		end
	end
end,"Freezes all droids")

Droid.NewSwitch("Anti invis",function(bool)
    aid = bool
    if bool then
        while wait() and aid do
            local currentroom = game.Workspace:FindFirstChild("Room")
            if currentroom ~= nil then
			for i,v in pairs(currentroom:WaitForChild("Enemies"):GetChildren()) do
       			if v:FindFirstChildWhichIsA("Humanoid") then
              		for i,p in pairs(v:GetChildren()) do
                    	if p:IsA("BasePart") and p.Name ~= "HumanoidRootPart" then
                         	if p.Transparency == 1 then
                        		p.Transparency = 0.6
                         	end
                        end
                    end
              	end
       		end
     		end
        end
    end
end,"Makes it so Ghost droids wont go invis")

Droid.NewSwitch("Anti bullets",function(bool)
    abd = bool
end,"Destroys all bullets in the game")

Room.NewSwitch("Auto Click",function(bool)
    akb = bool
    if bool then
        while wait(0.1) and akb do
            pcall(function()
            	local currentroom = game.Workspace:FindFirstChild("Room")
            	if currentroom ~= nil then
            		if not currentroom:FindFirstChild("VaultTable") then
            			for i,v in pairs(currentroom.Enemies:GetChildren()) do
            				if v.Name == "Button" and v:FindFirstChild("ClickDetector") then
             		   			fireclickdetector(v.ClickDetector)
                     		elseif v.Name == "MysteryButton" and v:FindFirstChild("ClickDetector") then
                       			fireclickdetector(v.ClickDetector)
            	    		end
           		 		end
        			end
     			end
   			end)
        end
    end
end,"Auto Clicks buttons in the current room.")

Room.NewSwitch("Anti Lava",function(bool)
    alr = bool
    if bool then
        while wait(0.1) and alr do
            pcall(function()
            local currentroom = game.Workspace:FindFirstChild("Room")
            for i,v in pairs(currentroom:GetDescendants()) do
            	if v.Name == "Lava" or v.Name == "Sand" or v.Name == "Acid" or v.Name == "Water" or v.Name == "Killer" then
                	if not v:FindFirstChild("Anti Damage") then
                    	local part = Instance.new("Part")
                    	part.Size = Vector3.new(v.Size.x +0.8, v.Size.y +0.8, v.Size.z +0.8)
                     	part.Position = Vector3.new(v.Position.x, v.Position.y +0.7, v.Position.z)
                      	part.Transparency = 1
                       	part.Anchored = true
                        part.Parent = v
                        part.Name = "Anti Damage"
                        part.Shape = v.Shape
                       	
                        if v.Name == "Lava" or v.Name == "Acid" then
                            part.Size = Vector3.new(v.Size.x +2.5, v.Size.y +2.5, v.Size.z +2.5)
                        end
                    end
                end
            end
        	end)
        end
    end
end,"Makes it so Lava/Sand/Acid/Water doesnt damage you")

Room.NewSwitch("Anti NegativeCircuit",function(bool)
    anc = bool
    if bool then
        while wait(0.01) and anc do
            for i,v in pairs(game.Workspace:GetChildren()) do
                if v.Name == "NegativeCircuit" then
                    v:Destroy()
              	end
            end
        end
    end
end)

Room.NewSwitch("Auto Grab",function(bool)
	agci = bool
    if bool then
        while wait(0.1) and agci do
            local currentroom = game.Workspace:FindFirstChild("Room")
            
            for i,v in pairs(game.Workspace:GetChildren()) do
                if v.Name == "Circuit" or v.Name == "BigCircuit" then
                	v.Position = lp.character.HumanoidRootPart.Position
                	v.CanCollide = false
                	wait(0.02)
                end
            	if v:IsA("Tool") and v:FindFirstChild("Handle") then
                	if v.Handle.Anchored == false then
                    	lp.character.Humanoid:EquipTool(v)
                    end
                end
            end
        	for i,v in pairs(currentroom:GetChildren()) do
            	if v.Name == "BerryBush" and v:FindFirstChildWhichIsA("Tool") then
                 	v:FindFirstChildWhichIsA("Tool").Handle.CanCollide = false
                	v:FindFirstChildWhichIsA("Tool").Handle.Position = lp.character.HumanoidRootPart.Position
                end
            end
        	for i,v in pairs(currentroom:GetDescendants()) do
                if v.Name == "Circuit" or v.Name == "BigCircuit" then
                	v.Position = lp.character.HumanoidRootPart.Position
                	v.CanCollide = false
                	wait(0.02)
                end
            end
        
        	for i,v in pairs(currentroom:GetDescendants()) do
            	if v:IsA("Tool") and not v.Parent:FindFirstChild("Enemy") then
                	lp.character.Humanoid:EquipTool(v)
                end
            end
            
            if currentroom:FindFirstChild("Handle") then
                currentroom:FindFirstChild("Handle").CanCollide = false
                currentroom:FindFirstChild("Handle").Position = lp.character.HumanoidRootPart.Position
            end
    	end
    end
end,"Auto grabs Items/Circuits in the room")

Room.NewSwitch("Auto Box",function(bool)
	ab = bool
    if bool then
        while wait(0.1) and ab do
            local currentroom = game.Workspace:FindFirstChild("Room")
           	for i,v in pairs(currentroom:GetChildren()) do
                print(string.split(v.Name, " ")[1])
                if string.split(v.Name, " ")[1] == "PressurePlate" then
                    print(v)
                    local b = currentroom:FindFirstChild("PuzzleBox "..string.split(v.Name, " ")[2])
                    print(b)
                    b.Box.CFrame = CFrame.new(v.Activator.Position.x,v.Activator.Position.y,v.Activator.Position.z)
                end
            end
        end
 	end
end)

World.NewButton("Make Day",function()
	game:GetService("Lighting").Brightness = 1
	game:GetService("Lighting").FogEnd = 999999
	game:GetService("Lighting").GlobalShadows = false
	game:GetService("Lighting").Ambient = Color3.fromRGB(235, 235, 235)
end)

World.NewSwitch("Anti Void",function(bool)
    antivoid.CanCollide = bool
end,"Makes it so u dont get damaged by The void.")

game:GetService("RunService").Stepped:Connect(function()
    antivoid.Position = Vector3.new(lp.character.Torso.Position.x, -2, lp.character.Torso.Position.z)
    for i,v in pairs(game.Workspace:GetChildren()) do
        if v.Name == "Bullet" then
        	v:Destroy()
    	end
    end
end)



game:GetService("RunService").RenderStepped:Connect(function()
    if EspEnemies then
        for i,v in pairs(game.Workspace:FindFirstChild("Room"):WaitForChild("Enemies"):GetChildren()) do
        	if v:FindFirstChild("Enemy") and not v:FindFirstChild("ZeouronChams") then
            	local hl = Instance.new("Highlight")
    			hl.FillColor = Color3.fromRGB(255,0,0)
       			hl.OutlineTransparency = 1
        		hl.Parent = v
          		hl.Name = "ZeouronChams"
            end
        end
    end
	if EspAllies then
        for i,v in pairs(game.Workspace.PassiveDroids:GetChildren()) do
        	if v:FindFirstChild("Enemy") and not v:FindFirstChild("ZeouronChams") then
            	local hl = Instance.new("Highlight")
    			hl.FillColor = Color3.fromRGB(0,255,0)
       			hl.OutlineTransparency = 1
        		hl.Parent = v
          		hl.Name = "ZeouronChams"
            end
        end
    end
	if EspPlayers then
        for i,v in pairs(game.Players:GetChildren()) do
        	if v.character:FindFirstChild("Humanoid") and not v.character:FindFirstChild("ZeouronChams") then
            	local hl = Instance.new("Highlight")
    			hl.FillColor = Color3.fromRGB(255,255,255)
       			hl.OutlineTransparency = 1
        		hl.Parent = v.character
          		hl.Name = "ZeouronChams"
            end
        end
    end
end)